matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      language: python
    - os: linux
      dist: trusty
      sudo: false
      language: python
    - os: linux
      dist: precise
      sudo: required
    - os: osx
script: df -h
